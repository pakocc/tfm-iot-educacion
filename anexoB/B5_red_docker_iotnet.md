# B.5. Red Docker iotnet
La red iotnet permite que todos los contenedores se comuniquen internamente sin exponer puertos adicionales al exterior. Esto mejora la seguridad y evita interferencias entre usuarios.

Para inspeccionar la red:

```bash
docker network inspect iotnet
```
